@using SBS_Ecommerce.Models.Base;
@{
    Helper helper = new Helper();
    SBS_Ecommerce.Framework.Utilities.BaseUtil cpUT = new SBS_Ecommerce.Framework.Utilities.BaseUtil();
    var pathTheme = cpUT.GetPathTheme();
    var slider = helper.DeSerializeSlider(Server.MapPath(pathTheme) + "/configslider.xml");
}
<!--------------- Hero area --------------->
<div class="hero-area">
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div id="hero-slider" class="hero-slider owl-carousel">
                    @foreach (var item in slider.LstPicture)
                    {
                        item.Path = Url.Content("~" + item.Path);
                        <div style="background-image: url('@item.Path')">
                          
                        </div>
                    }

                    @*<div style="background-image: url(/Content/Theme/Theme3/img/slider/slider2.jpg)">
                            <div class="slider-caption">
                                <h2>Big Sale</h2>
                                <h1>Up to 50% off</h1>
                                <a class="btn btn-default btn-big" href="#">Shop Now!</a>
                            </div>
                        </div>
                        <div style="background-image: url(/Content/Theme/Theme3/img/slider/slider3.jpg)">
                            <div class="slider-caption">
                                <h2>Big Sale</h2>
                                <h1>Up to 50% off</h1>
                                <a class="btn btn-default btn-big" href="#">Shop Now!</a>
                            </div>
                        </div>*@
                </div>
            </div>
        </div>
    </div>
</div>
